# Normalize line endings to LF in the repository
* text=auto

# Force LF for Atlas migration files so checksums are consistent
# across Windows (local) and Linux (CI/GitHub Actions)
packages/core/db/migrations/*.sql text eol=lf
packages/core/db/migrations/atlas.sum text eol=lf

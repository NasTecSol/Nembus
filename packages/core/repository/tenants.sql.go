// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: tenants.sql

package repository

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createTenant = `-- name: CreateTenant :one
INSERT INTO tenants (tenant_name, slug, db_conn_str, is_active, settings)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, tenant_name, slug, db_conn_str, is_active, settings, created_at, updated_at
`

type CreateTenantParams struct {
	TenantName string      `json:"tenant_name"`
	Slug       string      `json:"slug"`
	DbConnStr  string      `json:"db_conn_str"`
	IsActive   pgtype.Bool `json:"is_active"`
	Settings   []byte      `json:"settings"`
}

func (q *Queries) CreateTenant(ctx context.Context, arg CreateTenantParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, createTenant,
		arg.TenantName,
		arg.Slug,
		arg.DbConnStr,
		arg.IsActive,
		arg.Settings,
	)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.TenantName,
		&i.Slug,
		&i.DbConnStr,
		&i.IsActive,
		&i.Settings,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTenantBySlug = `-- name: GetTenantBySlug :one
SELECT id, tenant_name, slug, db_conn_str, is_active, settings, created_at, updated_at FROM tenants WHERE slug = $1 AND is_active = true
`

func (q *Queries) GetTenantBySlug(ctx context.Context, slug string) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantBySlug, slug)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.TenantName,
		&i.Slug,
		&i.DbConnStr,
		&i.IsActive,
		&i.Settings,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTenantBySlugAny = `-- name: GetTenantBySlugAny :one
SELECT id, tenant_name, slug, db_conn_str, is_active, settings, created_at, updated_at FROM tenants WHERE slug = $1
`

func (q *Queries) GetTenantBySlugAny(ctx context.Context, slug string) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantBySlugAny, slug)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.TenantName,
		&i.Slug,
		&i.DbConnStr,
		&i.IsActive,
		&i.Settings,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listActiveTenants = `-- name: ListActiveTenants :many
SELECT id, tenant_name, slug, db_conn_str, is_active, settings, created_at, updated_at FROM tenants WHERE is_active = true ORDER BY tenant_name
`

func (q *Queries) ListActiveTenants(ctx context.Context) ([]Tenant, error) {
	rows, err := q.db.Query(ctx, listActiveTenants)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Tenant
	for rows.Next() {
		var i Tenant
		if err := rows.Scan(
			&i.ID,
			&i.TenantName,
			&i.Slug,
			&i.DbConnStr,
			&i.IsActive,
			&i.Settings,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAllTenants = `-- name: ListAllTenants :many
SELECT id, tenant_name, slug, db_conn_str, is_active, settings, created_at, updated_at FROM tenants ORDER BY tenant_name
`

func (q *Queries) ListAllTenants(ctx context.Context) ([]Tenant, error) {
	rows, err := q.db.Query(ctx, listAllTenants)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Tenant
	for rows.Next() {
		var i Tenant
		if err := rows.Scan(
			&i.ID,
			&i.TenantName,
			&i.Slug,
			&i.DbConnStr,
			&i.IsActive,
			&i.Settings,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTenant = `-- name: UpdateTenant :one
UPDATE tenants
SET 
    tenant_name = COALESCE($2, tenant_name),
    slug = COALESCE($3, slug),
    db_conn_str = COALESCE($4, db_conn_str),
    is_active = COALESCE($5, is_active),
    settings = COALESCE($6, settings),
    updated_at = NOW()
WHERE id = $1
RETURNING id, tenant_name, slug, db_conn_str, is_active, settings, created_at, updated_at
`

type UpdateTenantParams struct {
	ID         uuid.UUID   `json:"id"`
	TenantName string      `json:"tenant_name"`
	Slug       string      `json:"slug"`
	DbConnStr  string      `json:"db_conn_str"`
	IsActive   pgtype.Bool `json:"is_active"`
	Settings   []byte      `json:"settings"`
}

func (q *Queries) UpdateTenant(ctx context.Context, arg UpdateTenantParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, updateTenant,
		arg.ID,
		arg.TenantName,
		arg.Slug,
		arg.DbConnStr,
		arg.IsActive,
		arg.Settings,
	)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.TenantName,
		&i.Slug,
		&i.DbConnStr,
		&i.IsActive,
		&i.Settings,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
